-- Выполняйте по одному заданию в psql. Это ваш учебный стенд.
-- Дата по умолчанию: 2026-09-12 включительно.

-- 1. Выведите ID, дату, срок и сумму каждого активного заказа.
-- Подсказка: сумма заказа = SUM(order_items.quantity * order_items.unit_price).

-- 2. Сколько заказано по каждому менеджеру?
-- Связь: orders.customer_id -> customers.customer_id.
-- Отменённые заказы исключите.

-- 3. Найдите сумму отгрузок каждого заказа на 12 сентября.
-- Отгрузки хранят количество, цена находится в order_items; связь по order_item_id.
-- Заказы без отгрузок тоже должны попасть в результат.

-- 4. Добавьте оплаты, остаток к оплате и статус оплаты.
-- Сначала отдельно агрегируйте отгрузки и платежи по order_id.
-- Не складывайте сырые таблицы через два JOIN.
-- У заказа 103 есть возврат; у 110 — переплата.

-- 5. Найдите просроченные неисполненные заказы на 12 сентября.
-- Ожидаемые ID: 102 и 104.

-- 6. Почему показатели «своевременно отгружен» и «отгружен» различаются?
-- Посмотрите исходный и текущий срок заказа 104, отгрузки заказа 103.

-- 7. Объясните дефицит компонента C-102.
-- Соберите открытое количество по каждой позиции; затем умножьте на BOM.
-- Обратите внимание: в заказе 102 две позиции требуют C-102.

-- 8. Без изменения базы: переключите дату дашборда с 12 на 16 сентября.
-- Проследите заказ 102: первая позиция отгружается 15-го, вторая остаётся открытой.

-- 9. Изменение с откатом. Оцените влияние дополнительного резерва.
-- Этот блок не сохраняет изменения.
BEGIN;
SELECT * FROM inventory WHERE component_id = 2;
UPDATE inventory SET reserved_external = reserved_external + 1
WHERE component_id = 2 AND reserved_external + 1 <= on_hand;
SELECT * FROM inventory WHERE component_id = 2;
ROLLBACK;

-- 10. Посмотрите план запроса, не выполняя изменение данных.
EXPLAIN
SELECT order_id, SUM(quantity)
FROM shipments
WHERE order_id IN (102,104)
GROUP BY order_id;
-- На небольшом количестве строк последовательное чтение нормально. Не ожидайте индекс
-- только потому, что он создан; планировщик оценивает стоимость.
